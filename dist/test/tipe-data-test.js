"use strict";
describe('Data Type', function () {
    it('should must declare', function () {
        const name = "Farhan";
        const balance = 10000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
